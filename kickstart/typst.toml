[package]
name = "zettyp-kickstart"
version = "0.1.0"
entrypoint = "lib.typ"
authors = ["Xinyu Xiang"]
license = "MIT"
description = "A forkable starting point for ZetTypst note system."
keywords = ["zettelkasten", "knowledge", "template"]

[template]
path = "template"
entrypoint = "index.typ"
