# ZetTypst

A Typst-native Zettelkasten note system, where the document language also serves as the semantic language of the knowledge system.

- `core/`: Graph and state descriptions and declarations
- `eval/`: A persistent incremental evaluation runtime
- `lsp/`: Editor integration and Typst wrappers for selected LSP capabilities
- `kickstart/`: An example configuration

[MIT](LICENSE).
